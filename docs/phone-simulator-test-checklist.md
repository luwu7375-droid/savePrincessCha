# 查手机 - Phone System Simulator 测试清单

## 实现完成状态 ✅

- ✅ Task #1: 数据库迁移 (20260625300000_fix_activity_log_constraints.sql)
- ✅ Task #2: 14屏手机模拟器完整实现
  - ✅ Step 1: HTML 结构 (index.html lines 621-851)
  - ✅ Step 2: CSS 样式 (v2.css lines 3567-4066)
  - ✅ Step 3: JavaScript 导航逻辑 (modules/phone.js)
  - ✅ Step 4: 初始化函数接线 (modules/phone.js init())

## 功能测试清单

### 1. 入口点测试
- [ ] 在 Playground 页面点击"查手机"卡片
- [ ] 手机模拟器弹窗正确显示
- [ ] 默认显示锁屏界面（紫色渐变背景）

### 2. 锁屏 → 主屏幕
- [ ] **移动端**：向上滑动锁屏解锁
- [ ] **桌面端**：点击锁屏解锁
- [ ] 主屏幕显示 8 个应用图标（4x2 网格）
- [ ] 底部显示搜索图标

### 3. 浏览器应用
- [ ] 点击"浏览器"图标进入浏览器屏幕
- [ ] URL 输入框正确显示
- [ ] 输入 URL 并点击"前往"按钮
- [ ] 显示"加载中…"状态
- [ ] 成功获取内容后，历史记录区域显示结果
- [ ] 错误处理正确显示（SSRF、超时、格式错误等）

### 4. 浏览历史时间线
- [ ] 打开浏览器后自动加载今日活动记录
- [ ] 历史记录按时间倒序显示
- [ ] 每条记录显示：标题、时间、URL
- [ ] 点击历史记录项触发 `window.openBrowserResult()`
- [ ] 摘要注入到聊天并关闭手机弹窗

### 5. 其他应用（静态界面）
- [ ] **通讯录**：显示联系人列表（KK）
- [ ] **信息**：显示消息线程预览
- [ ] **照片**：显示占位符
- [ ] **音乐**：显示播放器界面
- [ ] **设置**：显示设置项列表
- [ ] **电话**：显示通话界面
- [ ] **视频通话**：显示视频预览

### 6. 导航系统
- [ ] 每个应用顶部显示返回按钮（←）
- [ ] 点击返回按钮回到上一屏幕
- [ ] 导航历史栈正确维护
- [ ] 从应用返回主屏幕正确

### 7. 叠加层（Overlays）
- [ ] 点击搜索图标打开搜索叠加层
- [ ] 搜索叠加层覆盖整个屏幕
- [ ] 点击关闭按钮（×）关闭搜索
- [ ] 通知面板可以打开和关闭

### 8. 视觉样式
- [ ] 手机外壳：375x667px，圆角边框
- [ ] 状态栏：显示时间、信号、WiFi、电池
- [ ] 屏幕切换动画流畅（transform + opacity）
- [ ] 应用图标点击有缩放反馈
- [ ] 底部主页指示器显示

### 9. 响应式设计
- [ ] 移动端（≤480px）：手机外壳全屏显示
- [ ] 桌面端：手机外壳居中，保持固定尺寸
- [ ] 触摸手势在移动设备正常工作
- [ ] 鼠标点击在桌面正常工作

### 10. 数据库集成
- [ ] 浏览记录保存到 `cha_activity_log` 表
- [ ] `action_type = 'web_browse'`
- [ ] `source_type = 'phone_simulator'`
- [ ] `want_to_share` 字段接受任意文本（TEXT 类型）
- [ ] 查询今日记录使用时区感知的日期范围

### 11. 错误处理
- [ ] WEB_API_ENDPOINT 未配置时显示提示
- [ ] 网络错误显示友好消息
- [ ] SSRF 拦截显示"这个地址不能访问"
- [ ] 超时显示"网页加载超时了"
- [ ] 不支持的内容类型显示相应错误

### 12. 关闭和清理
- [ ] 点击弹窗外部关闭手机模拟器
- [ ] 点击关闭按钮关闭手机模拟器
- [ ] 关闭后重新打开，重置为锁屏状态
- [ ] URL 输入框清空

## 技术验证

### JavaScript 初始化
- [ ] `initGestures()` 正确绑定触摸事件
- [ ] `initAppLaunchers()` 绑定所有应用图标
- [ ] `initBackButtons()` 绑定所有返回按钮
- [ ] `initBrowser()` 绑定 URL 输入和前往按钮
- [ ] `initOverlays()` 绑定搜索和通知面板

### 状态管理
- [ ] `currentScreen` 正确跟踪当前屏幕
- [ ] `screenHistory` 正确维护导航栈
- [ ] `showScreen()` 正确切换屏幕显示
- [ ] `navigateToScreen()` 正确推入历史
- [ ] `navigateBack()` 正确弹出历史

### API 集成
- [ ] `handleReadUrl()` 正确调用 WEB_API_ENDPOINT
- [ ] 请求包含 Supabase 认证头
- [ ] `saveLog: true` 参数正确传递
- [ ] 响应数据正确解析
- [ ] `loadTodayActivity()` 正确查询数据库

## 已知限制

1. **静态应用**：通讯录、信息、照片、音乐、设置、电话、视频通话目前只是静态界面
2. **搜索功能**：搜索叠加层已实现 UI，但搜索逻辑未实现
3. **通知功能**：通知面板已实现 UI，但通知逻辑未实现
4. **音乐播放**：音乐界面已实现，但播放控制未实现

## 下一步计划（可选扩展）

- [ ] 实现搜索功能（搜索联系人、应用、历史记录）
- [ ] 实现通知系统（显示真实通知）
- [ ] 连接音乐播放器到实际音频源
- [ ] 实现照片库（显示用户上传的图片）
- [ ] 实现通讯录管理（添加、编辑联系人）
- [ ] 实现消息功能（发送、接收消息）
- [ ] 添加更多手势支持（下拉刷新、侧滑等）

## 测试环境

- 浏览器：Chrome/Safari/Firefox 最新版本
- 设备：桌面 + 移动端 (iOS/Android)
- 分辨率：375px - 1920px 宽度
- 数据库：Supabase PostgreSQL
- 后端：Supabase Edge Function (web.ts)

---

**最后更新**: 2026-06-25
**版本**: v2 build 20260623
**状态**: ✅ 核心功能完整实现，可以开始用户测试
