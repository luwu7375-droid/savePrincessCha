// _shared/music-service.ts
// Stub for music / audio tool functions.
// Reserved for future MCP migration — no functional implementation yet.
// When MCP is wired up, these will become tool calls instead of direct fetches.

export function getToolDefinitions() {
  return [];
}
